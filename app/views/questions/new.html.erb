<%= form_for(@question) do |f| %>
  <%= render 'shared/error_messages' , object: f.object %>
  <div class="form-group">
    <%= f.label :質問内容 %>
    <%= f.text_area :content, class: "form-control" %>
    <br>
    <%= f.submit "質問する", class: "btn btn-outline-primary" %>
  </div>
<% end %>

